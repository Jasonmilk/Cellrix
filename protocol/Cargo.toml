[package]
name = "cellrix-protocol"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "CommonIntents协议栈的Rust实现：CIS/CIB/CAP数据结构与宽容解析器"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"

[dev-dependencies]
# 用于测试
serde_json = "1.0"
