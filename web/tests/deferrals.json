{
  "_why": [
    "TWO KINDS OF ENTRY, NEVER MIXED (putting them in one field would be one-field-two-meanings):",
    "  requires  = the CAPABILITY is not present; a probe decides, and it releases by itself the day the capability appears.",
    "  deferrals = the CRITERION is unreachable; only a human rewrite releases it, so a retirement_plan is mandatory.",
    "UNREGISTERED => unknown => BLOCKING. There is no global 'allow' switch, because a switch that exists will be used once.",
    "Format note: JSON rather than TOML because the runner is Node and must validate this with zero dependencies;",
    "the repo's TOML files are hand-parsed by python, and a second hand-parser for a third format is the defect, not the fix."
  ],
  "requires": [
    {
      "id": "cdp-browser",
      "probe": "cdp",
      "reason": "needs a real browser on the CDP port plus the live panel. Decided by PROBE, never by a date: a date is a guess, and 'the instance was never configured' must not read as 'the capability is absent'. NOTE (measured): fabricating a NEW capability name is already blocked — an unimplemented probe returns null, which classifies as UNKNOWN and blocks. What is NOT blocked is filing a suite here that does not need this capability, which is why an owner is now mandatory. The suite list is GONE on purpose: attaching a suite from here is exactly how a suite would be hidden. A `suites` key is now a REGISTER ERROR.",
      "owner": "jason"
    }
  ],
  "deferrals": [
    {
      "id": "recorded-10-period-chain",
      "suite": "prove_track_nodes_test.js",
      "owner": "jason",
      "expiry": "2026-11-30",
      "reason": "The criterion pins the DERIVED leaf id run-0537fb101ecccb5e. That recording was lost in the machine migration, and a hash cannot be inverted — so this is not 'not yet proven', it is UNREACHABLE: the criterion demands evidence that no longer exists in the world.",
      "retirement_plan": "Rewrite prove_track_nodes_test.js to assert SHAPE (a lineage of N periods) instead of the pinned derived leaf run-0537fb101ecccb5e, then delete the entry recorded-10-period-chain. Mutation proof REQUIRED first: breaking the lineage must turn it red, or the rewrite is only moved goalposts."
    }
  ],
  "pending": [
    {
      "id": "scan-probe-identity-family",
      "owner": "jason",
      "due": "2026-10-15",
      "retirement_plan": "scan-probe-identity-family: scan every probe in the chain for 'liveness instead of identity' (candidates: start-panel.sh ecosystem probe, up.rs poll_until, anaphase health) and give each an identity assertion; the panel probe is already fixed (id=\"uxApp\")."
    },
    {
      "id": "tdz-guard",
      "owner": "jason",
      "due": "2026-11-01",
      "retirement_plan": "tdz-guard: add a mechanical guard for use-before-definition in the shell/node scripts; this class has appeared three times (HERE, PORT_PANEL, run_all.js TDZ) and the guard must check USE before definition, not declaration order."
    },
    {
      "id": "paired-criteria-rule",
      "owner": "jason",
      "due": "2026-12-15",
      "retirement_plan": "paired-criteria-rule: make 'every blocking assertion needs a known-good positive control' a general rule of this register and of ADR-0047, not a one-off request, and add the positive controls it implies."
    },
    {
      "id": "paired-controls-for-register-rules",
      "owner": "jason",
      "due": "2026-11-15",
      "retirement_plan": "paired-controls-for-register-rules: give each register rule a known-good counterpart. Deleting the expiry check entirely leaves the default at exit 0, so today 'the rule fired' and 'the rule was satisfied' are indistinguishable for: deleted entry, platitude retirement_plan, expired deferral, missing owner, malformed pending."
    },
    {
      "id": "probe-identity-family-open",
      "owner": "jason",
      "due": "2026-10-30",
      "retirement_plan": "probe-identity-family-open: the chain-wide scan for liveness-instead-of-identity probes has NOT been done (only the panel probe is fixed). Carry this INTO the ADR as an open question plus the invariant 'having a signal is not being the thing', never as an answer — recording a conclusion that does not exist is the same family of error."
    }
  ]
}