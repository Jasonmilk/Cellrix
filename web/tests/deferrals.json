{
  "_why": [
    "TWO KINDS OF ENTRY, NEVER MIXED (putting them in one field would be one-field-two-meanings):",
    "  requires  = the CAPABILITY is not present; a probe decides, and it releases by itself the day the capability appears.",
    "  deferrals = the CRITERION is unreachable; only a human rewrite releases it, so a retirement_plan is mandatory.",
    "UNREGISTERED => unknown => BLOCKING. There is no global 'allow' switch, because a switch that exists will be used once.",
    "Format note: JSON rather than TOML because the runner is Node and must validate this with zero dependencies;",
    "the repo's TOML files are hand-parsed by python, and a second hand-parser for a third format is the defect, not the fix."
  ],
  "requires": [
    {
      "id": "cdp-browser",
      "probe": "cdp",
      "reason": "needs a real browser on the CDP port plus the live panel. Decided by PROBE, never by a date: a date is a guess, and 'the instance was never configured' must not read as 'the capability is absent'. NOTE (measured): fabricating a NEW capability name is already blocked — an unimplemented probe returns null, which classifies as UNKNOWN and blocks. What is NOT blocked is filing a suite here that does not need this capability, which is why an owner is now mandatory. The suite list is GONE on purpose: attaching a suite from here is exactly how a suite would be hidden. A `suites` key is now a REGISTER ERROR.",
      "owner": "jason"
    }
  ],
  "deferrals": [
    {
      "id": "recorded-10-period-chain",
      "suite": "prove_track_nodes_test.js",
      "owner": "jason",
      "expiry": "2026-11-30",
      "reason": "The criterion pins the DERIVED leaf id run-0537fb101ecccb5e. That recording was lost in the machine migration, and a hash cannot be inverted — so this is not 'not yet proven', it is UNREACHABLE: the criterion demands evidence that no longer exists in the world. INPUT IS DECLARED, NOT DISCOVERED (measured 2026-09-24): this suite flips between unproven (exit 3) and RED (exit 1) depending on what happens to be in .helix/events, because other runs write there. A scanned input is a hidden input; the shared events directory is now declared here and hashed into the gate report.",
      "retirement_plan": "Rewrite prove_track_nodes_test.js to assert SHAPE (a lineage of N periods) instead of the pinned derived leaf run-0537fb101ecccb5e, then delete the entry recorded-10-period-chain. Mutation proof REQUIRED first: breaking the lineage must turn it red, or the rewrite is only moved goalposts.",
      "depends_on": ".helix/events"
    }
  ],
  "pending": [
    {
      "id": "scan-probe-identity-family",
      "owner": "jason",
      "due": "2026-10-15",
      "retirement_plan": "scan-probe-identity-family: scan every probe in the chain for 'liveness instead of identity' (candidates: start-panel.sh ecosystem probe, up.rs poll_until, anaphase health) and give each an identity assertion; the panel probe is already fixed (id=\"uxApp\")."
    },
    {
      "id": "tdz-guard",
      "owner": "jason",
      "due": "2026-11-01",
      "retirement_plan": "tdz-guard: add a mechanical guard for use-before-definition in the shell/node scripts; this class has appeared three times (HERE, PORT_PANEL, run_all.js TDZ) and the guard must check USE before definition, not declaration order."
    },
    {
      "id": "paired-criteria-rule",
      "owner": "jason",
      "due": "2026-12-15",
      "retirement_plan": "paired-criteria-rule: make 'every blocking assertion needs a known-good positive control' a general rule of this register and of ADR-0047, not a one-off request, and add the positive controls it implies."
    },
    {
      "id": "paired-controls-for-register-rules",
      "owner": "jason",
      "due": "2026-11-15",
      "retirement_plan": "paired-controls-for-register-rules: give each register rule a known-good counterpart. Deleting the expiry check entirely leaves the default at exit 0, so today 'the rule fired' and 'the rule was satisfied' are indistinguishable for: deleted entry, platitude retirement_plan, expired deferral, missing owner, malformed pending."
    },
    {
      "id": "probe-identity-family-open",
      "owner": "jason",
      "due": "2026-10-30",
      "retirement_plan": "probe-identity-family-open: the chain-wide scan for liveness-instead-of-identity probes has NOT been done (only the panel probe is fixed). Carry this INTO the ADR as an open question plus the invariant 'having a signal is not being the thing', never as an answer — recording a conclusion that does not exist is the same family of error."
    },
    {
      "id": "reader-inconsistent-under-control",
      "owner": "jason",
      "due": "2026-10-20",
      "retirement_plan": "reader-inconsistent-under-control: the unconditional addition of a positive control made the declaration reader report ZERO matches while the same regex matched every file in isolation (per-file cap=\"cdp-browser\", 4/4), a bare test on a 20419-byte read was true, flags were identical (m), and there was no /g so no lastIndex reuse. All three candidate causes are REFUTED BY MEASUREMENT, so the fault lives in that patch's OWN construction (block placement/scope), not in the regex or the files. Re-land the three items ATOMICALLY - reader self-proof, then two-way join, then input sha - each one green, and verify the reader against an INDEPENDENT known-answer fixture (a minimal file with N declarations, assert N are read) instead of against itself."
    },
    {
      "id": "new-criteria-born-paired",
      "owner": "jason",
      "due": "2026-11-20",
      "retirement_plan": "new-criteria-born-paired: new criteria must be born WITH a known-good counterpart (hard rule); existing ones stay WARN (soft) so historical debt does not turn the gate red. This is the meta-rule that governs every other criterion, so a soft door here is a soft door on all of them."
    },
    {
      "id": "a11y-tree-projection",
      "owner": "jason",
      "due": "2026-11-25",
      "retirement_plan": "a11y-tree-projection: in ADR-0048 require that the semantic grid be projectable as an ACCESSIBILITY TREE. A screen reader is a silicon agent, and WAI-ARIA grid plus roving tabindex is the standardised navigable semantic grid; if the grid cannot project to that tree it is a parallel system beside ARIA and the existing agent tooling (screen readers, automation, a11y tests) cannot use it. aria-busy is also the standard loading representation."
    },
    {
      "id": "readiness-vocabulary-for-probes",
      "owner": "jason",
      "due": "2026-10-30",
      "retirement_plan": "readiness-vocabulary-for-probes: rename and re-scope the probe-family work using Kubernetes liveness vs readiness. The missing concept is READINESS ('alive' is not 'it is the thing and it is usable'); adopting the vocabulary makes the problem family explicit instead of being rediscovered per probe."
    },
    {
      "id": "rust-embed-dev-mode",
      "owner": "jason",
      "due": "2027-01-15",
      "retirement_plan": "rust-embed-dev-mode: replacing the hand-written include_str! embedding with rust-embed (debug reads from disk, release embeds) is a clear gain BUT it touches the verified chain (byte-equivalence plus build-with-the-binary). Register as an independent action item; do NOT do it while ADR-0048 is being written."
    },
    {
      "id": "trusted-types-two-layer",
      "owner": "jason",
      "due": "2026-12-10",
      "retirement_plan": "trusted-types-two-layer: adopt CSP require-trusted-types-for script plus a trustedTypes policy as the RUNTIME half of the innerHTML discipline, but do NOT downgrade the static check to 'assert the header is present'. Trusted Types is enforced only by Chromium engines; Firefox and Safari ignore the directive, so on those engines the runtime half does not exist and the static check remains the cross-engine backstop. Two layers, not a replacement."
    },
    {
      "id": "mutation-script-crash-safe",
      "owner": "jason",
      "due": "2026-10-25",
      "retirement_plan": "mutation-script-crash-safe: mutation runs must be IDEMPOTENT and CRASH-SAFE. Measured 2026-09-24: set -e aborted a verification script mid-way (the mutated runner deliberately exited 3) and the RESTORE never ran, leaving the register in the mutated state. Correct form is BOTH: trap ... EXIT for unconditional restore, AND a start-time assertion that the tree is at baseline; and the restore itself must be asserted afterwards. Explicit per-command capture alone is not enough because it relies on attention, which is the whole lesson of this family."
    },
    {
      "id": "probe-known-good-assertion",
      "owner": "jason",
      "due": "2026-10-25",
      "retirement_plan": "probe-known-good-assertion: the probe is a verification apparatus and it must itself be verified. requires-entries never expire because the probe re-decides reachability every run, but that argument assumes a HEALTHY probe: a probe that is broken or always answers absent leaves those suites held forever with a green gate and nobody watching. Add a known-good assertion (in an environment where the capability IS present the probe must report present); in the current environment CDP is absent so that assertion is itself held, and it must be REPORTED as held rather than assumed fine."
    },
    {
      "id": "input-isolation-private-copies",
      "owner": "jason",
      "due": "2026-11-30",
      "retirement_plan": "input-isolation-private-copies: declaration is not isolation. depends_on plus a sha makes drift VISIBLE, not IMPOSSIBLE, and the suites still flip between exit 1 and exit 3 while other runs write into the shared events directory. Give every suite a PRIVATE copy of its declared inputs (Bazel execroot / Nix / pytest tmp_path), and record the registered input sha so a green run is bound to a determinate input (lockfile semantics)."
    },
    {
      "id": "depends-on-all-suites",
      "owner": "jason",
      "due": "2026-11-15",
      "retirement_plan": "depends-on-all-suites: extend the mandatory depends_on declaration from deferrals to ALL registered suites, because the risk sits on the 20 proven ones too - they may read shared mutable state and merely happen to be green, with no sha and no signal. A suite with no inputs must declare 'none': declaring none is still a declaration."
    },
    {
      "id": "sentinel-red",
      "owner": "jason",
      "due": "2026-10-20",
      "retirement_plan": "sentinel-red: add an always-failing SENTINEL_RED suite as a permanent NEGATIVE control, and assert the gate reports it red AND that RED ROSTER names it with its exit code. This pairs the roster mechanism at zero cost: no mutation, no restore, and it cannot leave the tree in a bad state."
    },
    {
      "id": "hash-normalisation",
      "owner": "jason",
      "due": "2026-11-10",
      "retirement_plan": "hash-normalisation: normalise the input hash - sort paths, drop mtime, use relative paths, and prefer CONTENT hashing over name+size (name+size is stable but blind to a same-size edit). A hash containing mtime, traversal order or absolute paths produces FALSE drift, which is worse than no sha at all because it sends you chasing a drift that does not exist. See reproducible-builds.org, git hash-object, Bazel/Nix content addressing."
    }
  ]
}