# ADR-0048 索引（由 adr_index_test.js 生成，勿手改）

用法：按 **精确 §号** 取细则；本文件是常驻的导航层，不替代正文。

| § | 标题 | 形状 | 一句话 |
|---|---|---|---|
| 0 | 现行状态（**先看这一节**；旧条文按 §52.5 保留，不删） | B | > 本 ADR 已累积到 §62，其中若干裁定互相取代过。以下是当前生效的一组； |
| 1 | 同源两投影（本 ADR 的正式表述） | B | > 不存在两份事实，只存在一个权威态（ledger）。碳基与硅基各持一份投影。 |
| 2 | 管辖权：**扩展**既有所有者，不是"建立所有者" | B | 主人已经在：Cx.state.nav + setNav（唯一写入者），ADR-0022 N-003/N-009 已规定其唯一性与 URL 表达。 |
| 3 | 状态三分（借 `status` / `fetchStatus` 矩阵） | B | 0 / null / absent 必须可区分且一等公民。先例：Protobuf v3 取消 field presence 造成真实 |
| 4 | 渲染契约 | A | - 键控渲染（按 key 复用节点 + 单层 diff），不需要构建体系（~百行纯 JS，进 include_str! 资产）。 |
| 5 | 检测器标定（取代"判据成对"的说法） | A | 任何判据都要两侧标定：sensitivity（该红时红）+ specificity（不该红时不红）。 |
| 6 | 「声明优于发现」（declaration over discovery）——通用不变量 | A | 同一 bug 的三次实例化（规格：扫描驱动 ⇒ 集合/输入可变 ⇒ 静默错）： |
| 7 | 文件清单的机械检查 | B | § 顶部的清单写死;配一条检查：改动落在清单之外 ⇒ 红（配越界负例）。 |
| 8 | 验证装置需独立 fixture（自证 ≠ 对证） | B | - 被测物必须处于失败状态，证据才算数（在已修复状态上做证伪不算——实测过一次）; |
| 9 | 执行纪律 | B | 1. 改默认值求值顺序 / 往既有脚本插代码 ⇒ 必须真跑那条路径（不能用 --stop、旁路或静态检查代替）。 |
| 10 | 诚实边界 | B | 1. 面板的这条链证明的是「本机通」,不是「从零能通」; |
| 11 | 登记而非本轮解决（防第 1 条无限制生长） | B | 本 ADR 不解决以下项，只登记（各有 id/owner/due/retirement_plan）： |
| 12 | 后果 | B | - 得到：面板有了状态所有者（扩展而非新建）、渲染契约可机械检查、 |
| 13 | 管辖边界（**关注点声明**，清单只是它的可执行投影） | B | 边界是语义的，不是名字的。 一条静态文件清单必然被绕过——实现中总要碰清单外的文件。 |
| 14 | Tracer bullet：目标组件与完成定义（**冻结**） | BA | > 组件是竖切片，证明链也是竖切片。 |
| 15 | 闭环坐标与**格子级**竖切片（§14 的收窄） | CBA | 闭环的判据就是 DoD 那四条。 声明/工具/接口三层齐备不等于面板变好。 |
| 16 | Spike 章程：**给"够用"定一个数**（解除自我加码） | BA | 本 ADR 的提交门槛被层层抬高且从未显式复原：到 §15 时， |
| 17 | 格子判据 v2（五条，取代 §15.2 的草案） | CA |  |
| 18 | spike 的**最低自证**（否则结论强度低于任何一条正式判据） | CA | 实测 2026-09-24（选中该格的依据；若全部已绿 ⇒ 那格没病 ⇒ 换格子，这是选择逻辑不是失败）： |
| 19 | 判据 v3：粒度对齐围栏、修法不动协议（§17 的更正） | BA | 错误：判据限定"该格",基线却是文件级（innerHTML×10 · textContent×5 …）。 |
| 20 | 判据 v3 **重跑基线** + 动工顺序的更正 | A | 判据 2 的度量点由文件级改为该格路径,而旧数字（innerHTML×10）是文件级 |
| 21 | 巨人路径的**核实结果**及其对本格的约束 | CBA | > 来源：由审查方联网核实后提供（Flutter issue #145954 与 docs.flutter.dev、 |
| 22 | 硬矛盾修正：**三步**（4 并入 1）+ 第 0 步结论 + 轴与交点的次序 | A | 读取点（event_family.js:226/286、Cx.state.nav）在 DOM 侧的 JS 里。 |
| 23 | **三态代数**（先定义它，否则第 1 步注定红） | B | 这一格的值是多个事件累积的结果（tok += …）。三条事件分别为 0 / null / absent 时， |
| 24 | 代数的**实测复核**与三处修订（§23 之后） | B | > 来源：审查方在沙盒实跑（verify/t.js、t2.js、t3.js）。 |
| 25 | B′ 的**作用域**：下界只在求和处成立，派生量另立规则 | CB | 而 :338 e.tok / (maxTok || 1) 显示的正是比值，且 maxTok 因 :335 的比较坍缩 |
| 26 | 反直觉语义、NaN 盲区、断言次序、真实样本 | CA | 反例（决定性地支持本规则）：若把 absent 也算 partial,则每一个正常的缺失事件都会把整格标成残缺 |
| 27 | 三处收窄（快照绑定输入 · 基线分类 · 删除旧路径） | B | .helix/events 已在 ADR-0047 被确认为共享可变输入。 |
| 28 | `:338` 是**每事件份额条**，不是整格聚合比（**语义变更 ≠ bug 修复**） | B | [120, 80, 200] 现在显示 0.600 / 0.400 / 1.000（三条各不同）, |
| 29 | **两类「出声」必须分开**（§26.2 的作用域澄清，**不是新判据**） | C | 把 §26.2 的「不通过即报错/阻断」直读到渲染路径： |
| 30 | 计数器**拆成两个**（否则门会在诚实数据上变红）+ 快照用私有副本 | A | 若断言 dataUnknowns === 0： |
| 31 | 接线前的**预先登记**（快照的等价替代）+ 三步的已知中间态 + `:335` | BA | cdp=absent,而该格渲染在 include_str! 内嵌的单文件 HTML 里 ⇒ 没有浏览器就没有"当前实际输出"。 |
| 32 | 折叠/展开是**两条渲染路径**（实测）+ 判据 4 的诚实状态 | BA | 折叠  :215–:230   class="e-compact-btn"   →  g.tok（组的聚合）   … :230 continue |
| 33 | **`max` 的两个用途分开裁定**（§28.3 的直接应用，开工前必裁） | B | [120, null, 200]：当前 max = present(200) ⇒ 条 0.600 / 未知 / 1.000; |
| 34 | c2 直接索引 `r.states`（不重读事件）+ 三步的提交纪律 + 只看登记表 | B | 实测（审查方 verify/c2b.js）： |
| 35 | b 步不是删除：`:335` **同时写 `maxDur`**（实测确认） | B | 334  var maxDur = 0, maxTok = 0; |
| 143 | b 步不是删除：`:335` **同时写 `maxDur`**（实测确认）   ← 原为重复的 §35（按需寻址要求 § 号唯一，ADR-0048 §143） | B | 334  var maxDur = 0, maxTok = 0; |
| 36 | 完工判据必须用 `src`，不能用条宽 `w`（`1.2` 是几何层的单位元） | C | ⇒ 1.2 把"没有数据"伪装成合法最小宽度——与 || 0、|| 1 完全同族, |
| 37 | "看见"的形态 + 两处实测耦合（**不新增判据**） | B | a/b/c1/c2 都在 prove_track.view.js,改完没有东西能证明跑通。 |
| 38 | 真实样本诊断（**移回收尾**）+ 界的方向相反 + 真实数据的预期 | B | 验证契约（a/b/c1/c2 与 §31.2）不需要真实数据——四张登记样本足够; |
| 39 | 真实字段诊断：**两个轴都读错路径**（含"一处还是两处"的答案） | A |  |
| 40 | **两轴必须同一轮修**（决定性证据）+ `src` 是分区不是回退 | B |  |
| 41 | 隐藏输入**第三次**：批次构成（跨 period）—— 语义裁定 + `project` 的输入不变量 | - | > 这一格聚合的是「当前 period」。 |
| 42 | 语义裁定：`sum(output_tokens)`（并修正我此前的"三种都合理"） | - | 实测（审查方 verify/tokens.js）： |
| 43 | 抄标准时的两处偏差 + `sum` 的佐证 + `dur` 的单位 | - | ⇒ 误实现把显式 null 静默转成 absent——与 ||、== null 完全同族, |
| 44 | 施工期四条（完成定义 / 三计数 / throw 是发现 / 停止规则） | - | > cell_seen 真实样本上，折叠摘要的 tok 必须是 present(<真数>),不是 absent。 |
| 45 | 施工期第一个发现：**样本选择本身会取错**（§44.1 那条"`evs` 取错"的实例） | - | cell_seen 初版按文件名排序取"最新" ⇒ 取到 |
| 46 | 判据的时态、样本必须**声明**而非筛选、判据落在摘要上 | C | 「完成判据现在可以满足了」不等于「判据已满足」。 |
| 47 | 两个层次的绿必须分开 + 变异③ + 夹具层的未行使路径（追溯审计） | B | - 投影侧：tok = p:12 ✅（真实样本上已测出）; |
| 48 | 刷墙的**边界** + 次序理由 + **界面侧唯一收工判据** | A | :164 / :335 / :226 / :258。不是 ProveTrack 整格族、不是 dur 轴三态、 |
| 49 | 视图侧无第二读取点 + **崩溃必须与红分离** | B | project 现在同趟给出 durStates;barWidth(r, i) 按索引取 |
| 50 | `bars` 由 `project` 返回（错位不可表示）+ 元断言 + a 步粒度 + `src` 是系统状态 | B | barWidth(r, i) 要求"视图遍历的下标 == project 内部 events 的下标"。 |
| 51 | 三条口径（**动手前的最后裁定**） | A | 实测（pinned 样本 15 条）：整批 ⇒ tok = 12;组 A ⇒ 4、组 B ⇒ 4、组 C ⇒ 4 |
| 52 | **Codd 第四态：`inapplicable`**（摘要「4 还是 4 ≥」的答案）+ `foldedCell` | A | 实测： |
| 53 | 标记与计数的**作用域**（`≥` 的鉴别力、N/A 不进计数）+ **`:335` 的答卷** | B | 报告里 partial=false 却仍 maxDisplay=p:4 >= ⇒ bound 是按 union 行算的 |
| 54 | **裁定 A 重开 → A′（摘要 = session）** + a 步必须拆成 a1/a2 | - | - :333 S.session.forEach(… evs.push(e)) ⇒ evs 是整个 session; |
| 55 | a1 **是修 bug，不是重构**（实测）+ 快照终于可得 + 口径三更新 + a2 的第二理由 | - | 把 :333/:164/:226/:338/:339 的取值与格式化原样抄进 Node（不动生产代码），喂真实数据： |
| 56 | **修前捕获**（`cell_before.js`）——本轮第一件证据 | A | Feathers 的 Sensing Algorithm：先写明知错的期望 → 读实际值 → 再替换。 |
| 57 | `:164` 的循环对象（**裁定 A 恢复，A′ 撤回**）+ KAT 双检 + 博物馆文件豁免 | B | for (var i = 0; i < S.session.length; i++) {                 // 迭代 S.session（session 级） |
| 58 | **摘要与分母是不同作用域**（`project` 一次调用、两个视图）+ KAT 输入钉死 + 判据更正 | - | - dur 在末条 ⇒ 6.600 / 4.400 / 22.000（审查方）; |
| 59 | `turns` 视图 + **可加性**判据（第三次纠正"先写期望"）+ `turns[i].bars` | B | > sum(r.turns[i].tok) === r.sessionTok |
| 60 | **"只有一个 turn"是分组的单位元**（同族第三次）+ 两处分组出处 + 判据拆两条 | C | 错误变体"所有条都归 turn[0]"与正确实现逐位相同;可加性在 turns.length === 1 时恒等成立。 |
| 61 | 常量声明化（**0-**）+ 三条取值点断言 + `out` 必须整体替换 | BA | 「含 null」与「全不可用」在裸数字上分别是 4 与 0,都丢失了"不完整 / 不适用"。 |
| 62 | 折叠与展开**共享一个序列**（收敛 b / c2 的两种说法）+ 条数一致判据 | C | ⇒ 数值逐位相同——因为分母是整批（session）级,不随子集变。 |
| 64 | 里程碑表 + `:340` 带出的两件事（同一条不变量的**第五个位置**） | CBA | 实测：pendIdx = [0,3] 是整批下标;而裁乙后 T1 / T2 各只有 3 根条（段内 0..2） |
| 65 | **M0：把门对准目标**（`N` 的分类基线）+ 里程碑重排（目标放第一行） | BA | 21 proven / 5 held / 0 red 全部作用于我们自己写的文件—— |
| 66 | M0 的收尾：**元断言** + `||` 分类 + **M0.5 提前**（能看见,才谈得上验证） | - | 视图源码经 include_str! 编译进二进制（README 的 rust-embed / wasm32 / 热重载"均在账上、未完成") |
| 67 | M0.5 **撤回** + "模块自身即预览" + 两条线并行 + **M1 首笔失败（同族第七次）** | - | "让视图可独立加载以便看见"本质是往视觉一致性走;而这一格需要的是结构一致性 |
| 68 | **缺席型门 vs 在场型门** + 引用检查落地（随 M1，不作前置） | - | ⇒ 一个让面板崩的改动,两道门全绿。 |
| 69 | **M0+：门往下伸一层（成员）** + "路为什么长"（essence）+ 一次假阳性 | B | > *"I divide them into essence — the difficulties inherent in the nature of the software — |
| 70 | **第 7 个位置**：清单 ≠ 页面（真值源必须下移）+ 接口名从实现读出 | B | boot.rs 清单 → HTML 占位符 → Rust 替换表 → 页面真正拿到 |
| 71 | M0++ **不需要**（两条正则）+ 第 8 个位置自检**干净** + **M1 第一半落地** | B | 实测四种形态：四种都"编译 OK",其中三种脚本没上页面,两种留下未替换的占位符。 |
| 72 | **两个出处消掉一个**（视图累加删除）+ c1 三分支 + 种子就是单位元的第 9 个位置 | CBR | ⇒ T2 是原病在新层的复发：根因是累加种子—— |
| 73 | 状态**字面量**与**集合**两个出处（第 9、10 个位置）+ 我"修 bug 时把 bug 犯第二次" | B | 投影与视图各拼一次状态词汇 ⇒ 对不上时不崩、门全绿,而该格恒显示"未计量"。 |
| 74 | **筛选是分组的下游**（单一出处落地）+ `:340` 二选一 + `bars` 带值 + **M3 重定义** | B | 审查指出：我把分组（哪些算一段）与筛选（哪几段显示）合成了"一个顾虑"。 |
| 75 | **M3 的归属表**（b 之前定死）+ 上位依据 + 第 12 个位置的读法 | B | > 手写即可能写错,与"先登记期望"同族。表在 b 之前定死;b 之后不得再改归属。 |
| 76 | **M3 归属表按"服务哪一格"重排**（b 之前同笔）+ 两条通则 | BA | 生成方式：--emit-sites 出行+类,再向上找最近的函数声明（谁拥有它）。 |
| 77 | **宽度单位进名字**（`wPx` + `W_UNIT`/`W_RANGE`）+ b1 的三件事 | - | ⇒ B/C/D 都不是假设,都是"照着写最容易写成的样子",且都不崩。 |
| 78 | **范围断言无鉴别力** → 补"真的量出东西" + bars **1:1 与序列对齐**（裁定甲） | - | ⇒ MIN_W 本身就在声明范围内 ⇒"全塌"合法;空集合使"越界=0"恒真。 |
| 79 | 上位依据**引用原文** + b1 的**已读证目标**（本轮不动手,如实记） | - | CI-144 五条哲学逐条对位：理论支撑不猜测（引 OTel / PromQL / Codd）· 做对不做快（先验证后提交）· |
| 80 | **b1a**：`fmtTok(it.tok)` 是 c1 引入的**活 bug**（同族第三次）+ 取值器不 stale | CB | ⇒ 算术 / 拼接同坏（v*1 → NaN、${v} → [object Object]）,不抛错、门全绿 |
| 81 | memo **拆掉**（缓存即第二真值源）+ b1b/b2 **界线重画** + 人工查看留物证 | B | 按 S.session 身份键控：数组被替换 ⇒ 重算 ✓;原地追加（push 进同一数组） ⇒ |
| 82 | **memo 整体删除**（缓存 = 第二份真相）+ 门新增 **DERIVED-STORE** | BA |  |
| 83 | 门文件**确定性重写**（三次盲改失败之后） | BA | 我在 view_hygiene_test.js 上盲改连续失败三次（重复 let bad ×2、覆盖 direct 行、 |
| 84 | b1b 的**实质是删除**（不是换取值）+ 提交前脚本机械化 + M3 排除无关红 | A | ⇒ 只换取值而留下 maxTok/maxDur/v / Math.max(1.2, …)： |
| 85 | 待定错位在 **b1b 当场生效**（第 15 个位置）+ 删除前的一行**已答** | B | pendIdx (session): [0,3,7] |
| 86 | 删除**档位**（A/B/C 计数相同、后果不同）+ 身份**必须声明** + 删前枚举已答 | CBA | ⇒ 三档门全绿、计数下降相同 ⇒ "计数下降 ≠ 交付"换到了「删除档位」上。 |
| 87 | `:385+` 三问已答 —— **DOM 的条宽单位是 `%`，不是 `px`**（停下记账,不深挖） | BA |  |
| 88 | **判定：列网格**（引文即证据）—— 编码、下限、Σ=100 与 §77 的下游 | C | - 证据：直接引自已贴出的代码行得到的结论; |
| 89 | `n/a` 的**编码裁定**（长度编码值 · 颜色/文本编码状态）+ 五条裁定的外部依据 | CB | 声明式可视化规范把无效值当作显式配置项,并明确警告： |
| 90 | **两套编码不得并存**（b1b-0 同笔换常量）+ `n/a` 的唯一答案 + 跨道的机械判据 | CB | ⇒ §89 已判"长度编码不适用",而声明仍在 ⇒ 下一笔会同时持有两套编码 |
| 91 | **分母集合**（第 17 个位置）+ `absent` 同裁 + 两个概念必须声明 | B | ⇒ absent 行在 raw 里得到 0.0000% ⇒ 零填充对 sum 无贡献 |
| 92 | **三个通道**（位置 / 长度 / 颜色）+ `n/a` 列**不消失** + §77 断言须同笔改写 | BA | ⇒ 若"不在序列"＝"没有这一列" ⇒ 它不可见 ⇒ 两条冲突。 |
| 93 | `COL_MIN_PCT` **不是常量而是规则** + 降级 + 所择分叉须写明 | B | ⇒ 根因："可见"与"诚实排序"是两个约束,一个常量同时满足不了。 |
| 94 | 顺序缺口（`flexPct` 前移）+ 降级分支**必须被行使** + 常量**出处** + 门变化**预先声明** | B | flexPct 原排 -0.5,而 b1b-0 就要删 wPx 并改写 §77 断言 |
| 95 | **b1b-0 的投影侧落地**：三通道 + tick 规则（按**实际分配宽**推导）+ 降级**为投影状态** | - | allocate(rows) → { |
| 97 | **expand → migrate → contract**（三步走）—— ① 已落 | - | 这次失败的价值高于一次成功：precommit.sh 第一次真正拦下坏提交（PRECOMMIT FAILED — do NOT commit） |
| 98 | **"铺不下"是声明的状态**（第三个 `reason`）+ `gridCols` 改为**声明入参** | C | 降级夹具 n/a=250 ⇒ N=250 列;clamp 后每列 max(1.25%, 0.40%) = 1.25% |
| 99 | **加载冒烟**（BVT）+ **strict 预期红**（XPASS）+ ② 已落地（**预期红,已确认红**） | A | var CELL_PCT = 100 / GRID_COLS; 残留 ⇒ 加载期 ReferenceError,而 node --check 查不出 |
| 100 | 方向修正：**投影只出「权重 + 状态」,几何交回布局引擎** + **曳光弹** + ③ 的未完成交代 | BA | IEEE TVCG 2022《Where's My Data? Evaluating Visualizations with Missing Data》： |
| 101 | ③ **第三次未完成**（败因在测试耦合）+ ③a/③b 拆分 + M6 登记 | BA | 两次连败都崩在"迁测试"上。根因： |
| 102 | META 只查"存在"不查"被求值" + 旧账（每项配变异）+ ③a **第四次未完成** | B | META 仅做文本匹配 ⇒ 断言写错、抛错、甚至被 if(false) 包住,都可能"存在但不跑"。 |
| 103 | 配方**验证通过**：逐字锚点 4/4（凭记忆 1/5） | CB | 同一次改动,两种锚点来源,结果差 4 倍： |
| 104 | ③a **迁完**（残留 0）+ META 变异的**真结论**：守卫不能写在它守护的文件里 | A | 剩余三处（158 · 258–260 · 276）用本次会话逐字打印的文本做锚点 ⇒ 3/3 命中 |
| 105 | **③b 完成**：像素编码删除,**红→绿即证据**（expand → migrate → contract 走完） | - | ⇒ ok EXPECTED-RED until ③: no pixel encoding coexists with the percentage encoding |
| 106 | 审查吸收：**结构转绿 ≠ 值真** + 三个钉死结果 + `b1b-1` 的六个单位元 | B | ⇒ 加载冒烟 bars: 3 | turns: 1 是形状冒烟（有几个）,不是值冒烟（是什么）—— |
| 107 | 审查吸收（五条全收）+ **b1b-1 的落笔计划** + 四项钉死模板 | B | 原判据"非 NaN、非恒 0"是值域形状判据,而且更糟： |
| 108 | 第 2 步的**两个前置**：`cols` 等长（已补）+ **分配量必须声明**（记账,不在本笔补） | A | ⇒ out.tok 与行单元格的值都已同源于投影 ⇒ 第 1 步无需新改动。 |
| 109 | **目标与里程碑（对齐）** —— 一句话目标、完成定义、已证/未证、六个单位元、下一步 | BA | > 这一格在真实数据上恒为 0/NaN ⇒ 修到显示真值,且值住在 project() 里、不住在 DOM 里。 |
| 110 | `0e` **裁决采纳** + 路线图对齐（含一处事实更正） | B | ⇒ 推论（对手头有利）：equal 模式下 1.2 / PEND_MIN / toFixed(4) 语义上全部无意义 |
| 111 | **0e 完成**：长度通道的量在**调用点声明**（两模式 + 缺省即抛） | A | ⇒ opts.mode 缺失即抛（没有任何静默缺省）： |
| 112 | **阻塞项钉死**：这一格是**两个出口**,不是一格（`12` 属于 tok 出口） | BA | 0e 声明 lane 的长度通道 = 时长（value）或关闭（equal）; |
| 113 | **step 2 落地**：视图第一次消费 `allocate`（计数判据达成）+ 一处**被静默丢掉的语义** | - | 调用点的声明： |
| 114 | 生态对齐：**R2 是第三条根因**（证轨在 Drive 下结构性为空）+ 四个裁决 + 与本地进展的和解 | CB |  |
| 115 | `A0` 的事实底座（跨仓端口普查,我自查）+ 两处新发现 | B |  |
| 116 | **step 2 的 regression**（全 present ⇒ 泳道全空）+ 规则⑫（数值守卫不得强制转换） | - | allocate([P(5), P(3), P(2)], {gridCols:200, mode:'value'}) |
| 117 | `A0` 落地：**端口表单一来源** + 撞车已消除 + 跨仓断言（自带变异） | B | Helix-Mind/docs/helixECO/ports.json —— 每个组件的 端口 / 协议 / 必填类别 只在这里声明: |
| 118 | `A0-fix`：**单一来源自己先成立**（路径/崩≠红/规则⑫成类/空行/一处驳回） | CBA | const ROOT = '/Users/jason/Developer/Jasonmilk' ⇒ 在别的机器上读不到即抛异常 ⇒ 退出 1 |
| 119 | **A1（R1）重新定性**：词表**早已补过**,落后的是**协议文档** | B | ⇒ K11 那条「不在词表 ⇒ 装配层拒收 ⇒ 恒 null」的机制,在我的树里已经不成立（Cellrix 侧 2026-09-15 已升到 1.1.0）。 |
| 120 | A2 的形状裁决（①+②,③拒绝）+ A1 判据三处补强 + **撞名已改** | CB | 审查方实测（我复核一致）： |
| 121 | **A1-a 落地**：文档词表 == 代码词表（双向 + 差集 + 机械抽取 + 声明式 SKIP） | CB |  |
| 122 | 🎯 **值判据第一次被行使**（b1b-1 + b1b-2 一笔完成）—— 判词失效 | A |  |
| 123 | 值判据**收口**：崩≠红 + 全样本广度 + 判据更诚实 + **干净的红** | A | value_criterion_test.js 加 uncaughtException ⇒ exit 4（断言失败仍 exit 1）。 |
| 124 | **A2（Cellrix 侧）**：`modeOf()` 三态 —— **运行模式只可能是"声明的"或"未知",绝无默认** | - | var MODE_PATHS = ['/data/mode', '/mode']; |
| 125 | 🎯 **M3 REACHED** —— 本格门 `ASSERTED TOTAL 0`,整门只剩一个**与本格无关**的既有红 | A | ⇒ 删除的理由不是"数字要降",而是"这些值在 step 2 就已经不住在视图里了"—— |
| 126 | **M3 的最后一项（人工查看）**：仪器已就位并**真跑过一次**,但**尚未行使**——如实 | BA | - 把"切到 Actual time 看三泳道"做成断言（render_test.js，jsdom 真渲染 + 活面板）; |
| 127 | 收口：`renderLanes` **是公开入口**（所以"人工查看"可被驱动）,而**驱动它的那一行没落到 jsdom 上** | BA | ⇒ 而 #eTraj 默认 display:none（prove_track.html:9）⇒ 泳道在用户打开检查器之前是完全不可见的 |
| 128 | 审查吸收：**已兑现的判据不在门里**（最重）+ fixture 入版本控制 + 它当场抓到两条 | A | ⇒ 理由（审查方 §6）：运行时语料 *.jsonl 按生态规则不进 git ⇒ 判据在新克隆上会 SKIP, |
| 129 | `run_all.js` 的**目的与价值**：把"缺席"变成可见——而这次的缺席正是**我自己造的** | - | > 它自己写着「ABSENCE IS NOT A PASS」:一个从不执行的套件读起来像覆盖率。 |
| 144 | `run_all.js` 的**目的与价值**：让"缺席"可见——而这次的缺席是我自己造的   ← 原为重复的 §129（按需寻址要求 § 号唯一，ADR-0048 §143） | - | > 它自己写着「ABSENCE IS NOT A PASS」：一个从不执行的套件读起来像覆盖率。 |
| 130 | 主机绝对路径：**从"我改了这一处"变成"这一类不可能再有"** | - | 实测三个文件仍可用：pinned_expectation.js ⇒ expectedTok 12 + 独立性自检 ok;cell_before.js ⇒ exit 0;precommit ⇒ PRECOMMIT OK。 |
| 131 | **改判：`null` 毒化是 bug,不是语义分歧**（审查方三条证据全部成立） | - | ⇒ 我此前把它登记为"语义决定 / 预期红"是错的。 ⇒ 这正是审查方 §3 三问要防的: |
| 132 | **修复的副作用：病从 `add` 搬到了 `maxPartial`**（审查方实测成立）——已修+逐条分类 | A | ⇒ 同一认知处境,两个答案 —— 正是刚修好的那个病,只是换了一层:从 add 搬到了 maxPartial。 |
| 133 | **改判："留红"是错的** —— 合取式拆开 + 规则⑭ + 两条新数学事实 | B | 第 7 条原文是一个合取式： |
| 134 | **下界的前提是"非负"——因此把非负变成声明,把违例变成"不可适用"** | BA | sum 侧：S + u ≥ S   ⟺   u ≥ 0        ← 前提:非负 |
| 135 | **规则⑫ 的镜像洞**：`P()` 无域守卫 ⇒ 字符串拼接进总和（假陈述）+ §24.4 变成输出谓词 | BA | P('5')                    ⇒ 被接受 |
| 136 | **共用字形**才是"结果取决于内容"的根因：`Pstr` 独立为 `'ps'` + 算术入口断言 | BA | add(Pstr('drive'), P(1))  ⇒ {k:'n'}       ← 静默 |
| 137 | **规则⑮ 的下半**:`else` 兜底是"连外观都不看"——三处穷尽化（A2 之前必修） | CB | ⇒ A2 要做的事,正是让 modeOf() 的 k:'ps'(drive/partner/survive)进入视图 ⇒ |
| 138 | **穷尽化的穷尽**:`allocate` 原是白名单（只拒 `ps`）⇒ 第五种字形静默占一列 | CB | allocate([{k:'zz'}, P(5)])  ⇒ cols=[0.5, 99.5] · state='ok' · reason=null |
| 139 | **白名单做门,黑名单做门内的警报器** —— 四条定理落成可执行判据（P1/P3/P4） | B | > 白名单本身不保证安全不变量 S: ∀x, accept(x) ⇒ handled(x)。 |
| 140 | **"handled" 必须**可分辨**（定理 2 的第三支）—— 我第三次中在同一处新装的墙上 | CAR | allocate([{k:'n'}]) ⇒ {state:'unavailable', reason:'no-present-rows', cols:[0.5], …} |
| 141 | **通则⑰ 声明最小律**（可派生者不得声明）+ 判据面第四代（∀ 而非举例） | CB | 把新字形 xx 真的加进 SHAPES（只改这一处,即最该改的地方）: |
| 142 | **增长实验做成常设探针**（A/C/D 全部转红）—— 验收标准达成 | CB |  |
| 145 | **按需加载,不占上下文注意力**：导航层由正文**生成**（45× 压缩）+ 一处被自己判据抓到的缺陷 | CB | 本 ADR 已达 6736 行 / 143 节 ⇒ 每次审查重读一段,就要为整段付上下文成本。 |
| 146 | **常驻层的预算**：政策写一次、测量派生、**审查必须存在**（phyt-DNA 审查的样板） | B |  |
| 147 | **退出码是契约**（通则⑳ 落地）：红 / 环境缺失 / 声明缺席 / **崩** 必须分账 | - | 同一个门在两台机器上读数不同: |
| 148 | ⚠️ **我提交在一个"未归因的计数"上**（一次流程失误,如实记） | BR | 提交那一刻的门读数:  2 red, 31 proven |
| 149 | **假红：它惩罚正确流程**（由 §148 的纪律当场揪出并修复） | - | adr_boundary_test.js 用的是 git diff --name-only（只看未暂存）。 |
| 150 | **幽灵红的归因完成**：`exit 3` 掉进 `failed`（只在**干净树**上出现的红） | A | §148 打印红名单   ⇒ 知道是 adr_boundary_test.js（此前只读计数,不知是谁） |
| 151 | **第三次归因完成：它不是幻影红**（附更正：越界者只是索引的位置） | B | BOUNDARY VIOLATION: <files> — outside the governed concern (presentation-layer state and rendering) |
| 152 | ⚠️ **提交信息声称了一个没有落地的补丁**（第二次同类失误,如实记） | B | 8527c3c 的信息写着「§13 投影加入索引路径」,而那次 Python 补丁因嵌套引号语法错而中止 |
| 153 | **step 3 落地**：缓存装**投影签名**而不是**渲染产物** + 判据 SKIP 的**结构性原因** | BA | /* 旧:缓存里装的是渲染出来的 HTML 字符串 —— 产物成了它自己变化检测的输入 = 第二份真相 */ |
| 154 | **我 §151.6 的诊断错了**：它不是崩,是**真红**——而真病是"门读了仓库外的输入" | BA | ⇒ 故我计划的那一笔是空的。⇒ 这是一次"自称的证据":我把没有核过的机制写进了 ADR |
| 155 | **真相：我引了一个不存在的 ADR**（两边都判错了;而错的方式不同） | A | FAIL  every ADR reference resolves somewhere in the ecosystem |
| 156 | ⚠️ **我修了 1/5,并且在它仍红时提交了**（§152 那一类的第二次） | - | ADR-00xx（尚未创建） 在正文里出现 5 次,我只替换了 1 处 ⇒ adr_anchor.js 仍然红; |
| 157 | **同一条判据,两种调用方式,两个结论**（本族第 9 次现身） | CA | cd Cellrix && node tools/adr_anchor.js          ⇒ exit=1（15 passed, 1 failed） |
| 158 | **"未声明的输入 ⇒ 不可识别"** —— 我接受这条裁定,并按它改顺序 | CBA | V = f(R, E)（R=仓库内容,已版本化;E=父目录内容,未声明）。 |
| 159 | **lockfile 模式**：把"生态里有哪些 ADR 号"作为派生产物**提交进版本控制** | BA | 注入一个不存在的引用（正是该判据存在的唯一理由要抓的东西）: |
| 160 | `STALE` 会说话了 —— 而 `--refresh` **产出了一张让检查失败的表**（如实） | BA | stale 此前只进了一个从未被打印的 source 字符串 ⇒ 注释说"a diff is a DECLARED stale", |
| 161 | ⚠️ **lane 值判据写好了,但**跑不动** —— 面板服务的是**未替换占位符**的页面 | BA | ⇒ 两级证明的形状（数学上诚实的组合）:投影 == 独立手算的 oracle（value_criterion_test.js,已绿） |
